fastapi==0.138.0
uvicorn[standard]==0.49.0
python-multipart==0.0.32        # security: fixes the multipart DoS / file-write / smuggling CVEs
starlette==1.3.1                # security: pinned floor (FastAPI's web core; clears starlette CVEs)
httpx==0.28.1

# Reasoning LLM clients (provider chosen at runtime via AETHER_LLM_PROVIDER).
# Range pins: both SDKs keep a stable surface within these bounds, so a fresh
# build picks up patches without breaking. openai also serves DeepSeek + any
# local/OpenAI-compatible server; anthropic serves Claude.
openai>=2.44.0,<3
anthropic>=0.111,<1

faster-whisper==1.2.1
requests==2.34.2                 # security: .netrc leak + later CVEs fixed
kokoro-onnx==0.5.0
soundfile==0.14.0
numpy==2.4.6
PyJWT==2.13.0                    # security: algorithm-confusion / DoS / SSRF JWK CVEs fixed
bcrypt==5.0.0
pydantic==2.13.4
pydantic-settings==2.14.2
asyncpg==0.31.0
redis==8.0.1
tzdata==2026.2          # zoneinfo database (slim image has no OS tzdata) for AETHER_TZ
