[pytest]
# Tests live under backend/tests and import the app as a top-level package
# (`from app.safety import ...`), so put the backend dir on the path.
pythonpath = .
testpaths = tests
addopts = -q -ra
