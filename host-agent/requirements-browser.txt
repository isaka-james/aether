# Optional extra for the browser/YouTube playback skill. The agent CORE stays stdlib-only;
# this is installed into a project-local venv (host-agent/.venv) by scripts/setup-browser.sh.
playwright>=1.49
